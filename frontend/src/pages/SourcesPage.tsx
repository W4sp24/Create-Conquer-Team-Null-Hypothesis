import Placeholder from '../components/Placeholder'

export default function SourcesPage() {
  return (
    <Placeholder
      label="Sources"
      title="Manage your knowledge base"
      body="NotebookLM-style source manager — curated global KB (read-only) on the left, org uploads with drag-drop and delete on the right. Not built yet."
    />
  )
}
